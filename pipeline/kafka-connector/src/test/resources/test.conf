include "base-test.conf"

kafka {
  input.topic = "flink.test"
  groupId = "flink-kafkaconnector-group"
  producer {
    max-request-size = 5242880
  }
}

task {
  consumer.parallelism = 1
  downstream.operators.parallelism = 1
}